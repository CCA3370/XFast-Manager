# Doctor — Synthesized Check Catalog (from 96 researched problems + 62 verdicts)

Verdicts applied: dropped false-positive-prone checks (ortho_over_default [dup of needs_sort],
multiple_providers_conflict, cycle_parse_failure, antivirus_blocking, vram_insufficient, hdr,
multi_monitor, ssaa_enabled, max_texture_quality_on_low_vram, stale_xplane_version,
default_data_outdated, excessive_scenery_count, flywithlua_scripts_disabled, beta_addon_on_stable).
Corrected severities/tiers per verdicts.

Orchestration: FRONTEND store. Local checks run on open; network checks stream after.

## Backend reuse map (NO new backend)
- analyze_xplane_log -> last_session_crashed, high_log_errors, plugin_errors, duplicate_plugin,
  missing_support_file, plugin_manager_internal_error, deprecated_dataref, dsf_errors,
  out_of_memory, heavy_memory_pressure, severe_texture_downscale, runloop_backlog,
  nvidia_permission, intel_unsupported(gpu_model), shadercache(+crash), reshade(third_party_blocked)
- analyze_crash_report -> crash_minidump, plugin_crash, gpu_driver_crash, overlay_interference, memory_exhaustion
- get_scenery_manager_data -> needs_sort, missing_libraries, duplicate_tiles, duplicate_airports,
  corrupted_ini(parse/empty), global_airports_disabled (entries)
- sort_scenery_packs -> SAFE fix for needs_sort / global_airports
- lookup_library_links_remote -> resolve missing_libraries download URLs
- airport_flatten_list_overrides / airport_flatten_apply_all_drifted -> flatten_drifted (SAFE fix)
- scan_output_cleanup_items / clean_output_items -> cleanable_caches (SAFE/confirm), old_crash_dumps
- scan_aircraft/scan_plugins + check_aircraft_updates/check_plugins_updates/check_scenery_updates -> addon_updates (network)
- gateway_list_installed / gateway_check_updates -> gateway_updates (network)
- check_for_updates -> app_update (SAFE) (network)
- is_xplane_running -> gate fs-mutating fixes
- validate_xplane_path -> xplane_path_invalid (gating)
- get_activity_log -> recent_install_failures

## NEW backend (doctor.rs) — pure detection only
1. doctor_scan_environment(xplanePath) -> DoctorEnvironmentReport {
     freeBytes,totalBytes,inProgramFiles,isSteamInstall,missingCoreDirs[],readonlyCount,
     injectors[](ReShade.ini/reshade-shaders/dxgi.dll/d3d11.dll/vulkan-1.dll),
     competingOrganizers[](xOrganizer) }
2. doctor_navdata_status(xplanePath) -> DoctorNavdataReport {
     cycles[]{folderName,providerName,cycle,airac,effectiveDate,expiryDate,daysRemaining,status,source},
     cifpPresent, earthDatMissing[] }
   - expiry PRIMARY: parse Custom Data/cycle_info.txt "Valid (from/to): DD/MMM/YYYY - DD/MMM/YYYY"
   - fallback (only cycle.json): status=unknown (avoid false expiry)
   - status: expired if today_utc>=expiry; expiring_soon if 0<expiry-today<=7; else ok

## Frontend-derived (no backend, from analyze_xplane_log.system_info)
- xplane_beta: version string contains -b/beta/-d  (raw "12.4.3-b4-29478f69")
- gpu fields displayed from system_info

## Sections (flight-impact order)
Integrity(gating) -> Crashes/Log -> Navdata -> Scenery -> Plugins -> Performance -> Environment -> Disk -> Updates

## Auto-fix tiers
SAFE (one-click): sort_scenery_packs, airport_flatten_apply_all_drifted, clean shadercache only,
  app self-update.
CONFIRM (show diff + click): clean_output_items (caches), old_crash_dump delete, addon/gateway updates,
  disable duplicate/overlapping scenery, clear read-only.
DESTRUCTIVE (guide only, never auto): remove ReShade/xOrganizer, delete addon files.
GATE: block all fs-mutating fixes while is_xplane_running.
