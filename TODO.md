添加取消任务功能，在安装时下方显示一个红色按钮，点击后弹出确认框，用户确认后取消当前所有任务，再添加一个跳过按钮（取消按钮左侧），点击后弹出确认框，用户确认后跳过当前任务，执行下一任务。任务取消或跳过后，删除任务产生的文件。注意，对于干净安装或覆盖安装，在安装时跳过/取消任务均会导致原有文件丢失，要明确提示用户。可以在设置里面加一个使用原子安装的开关（使用折叠栏，展开后显示详细解释，按钮放在此栏标题上），开关默认关闭。确保实现原子安装功能（先解压（对于压缩包）/移动（对于文件夹）到与目标同盘的临时目录。接下来，如果是首次安装（目标文件夹还不存在），那就直接原子移动过去；如果是二次安装中的干净安装，就先把目标目录重命名（比如可以加个origin后缀之类的），然后把临时文件原子移动过去并校验，再把origin目录中对应需要备份（保留）的文件（夹）原子移动到当前新装文件夹中（涂装跳过已存在），最后再删除原始目录；如果是二次安装中的覆盖安装，也尽量用原子移动的方法来实现（但要保证新文件覆盖旧文件，并且不会删除不存在于新文件中的旧文件））。如果我的逻辑有问题，请随时指出并询问其他可行方法。高质量完成。